export class Pokemon {
  public id: Number;
  public name: String;
  public cp: Number;

  constructor(id: Number, name: String, cp: Number) {
    this.id = id;
    this.name = name;
    this.cp = cp;
  }
}
